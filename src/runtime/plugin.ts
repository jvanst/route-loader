import { defineNuxtPlugin } from '#app'

export default defineNuxtPlugin(async (nuxtApp) => {
  // console.log('Plugin by route-loader!')

  // nuxtApp.hook('app:created', (pageComponent) => {
  //   console.log(pageComponent)
  // })
})
